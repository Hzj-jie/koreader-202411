local gettext = require("gettext")
return {
  name = "calibre",
  fullname = gettext("Calibre"),
  description = gettext(
    [[Integration with calibre. Send documents from calibre library via Wi-Fi and search calibre metadata.]]
  ),
}
